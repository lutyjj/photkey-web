<%= form_with(model: like_log) do |form| %>
  <% if like_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like_log.errors.count, "error") %> prohibited this like_log from being saved:</h2>

      <ul>
        <% like_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :photo_id %>
    <%= form.text_field :photo_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
